Placeholder for financial models to reduce duplication.

For example, a single `CoxIngersollRoss` instance can then have `Instrument` and `StochasticProcess` implemented upon it, instead of two separate structs.